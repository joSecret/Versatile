$schema: https://git.drupalcode.org/project/drupal/-/raw/10.1.x/core/modules/sdc/src/metadata.schema.json
name: Content layout sidebar
status: experimental
description: 'Content layout sidebar description'

props:
  type: object
  properties:
    comp_att:
      type: Drupal\Core\template\Attribute
      title: Component attribute
      description: Wrapper component attributes

    slotHeader_att:
      type: Drupal\Core\template\Attribute
      title: Component header attribute
      description: Wrapper component header attributes

    slotSidebar_att:
      type: Drupal\Core\template\Attribute
      title: Component attribute
      description: Wrapper component attributes

    slotContent_att:
      type: Drupal\Core\template\Attribute
      title: Component attribute
      description: Wrapper component attributes

    comp_classes:
      type: array
      items:
        type: string
      title: Content layout sidebar classes
      description: An array of utility classes. Use to add extra classes or custom CSS classes over to this component.

    slotHeader_classes:
      type: array
      items:
        type: string
      title: Content layout header classes
      description: An array of utility classes. Use to add extra classes or custom CSS classes over to this component.

    slotSidebar_classes:
      type: array
      items:
        type: string
      title: Content layout sidebar classes
      description: An array of utility classes. Use to add extra classes or custom CSS classes over to this component.

    slotContent_classes:
      type: array
      items:
        type: string
      title: Content layout sidebar classes
      description: An array of utility classes. Use to add extra classes or custom CSS classes over to this component.

slots:
  header:
    title: sidebar
    description: Header slot description.
  sidebar:
    title: sidebar
    description: Sidebar slot description.
  content:
    title: Content
    description: Content slot description
